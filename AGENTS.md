Goal: Improve a Streamlit football analytics app using StatsBomb Open Data.

Non-negotiables:

Don’t break existing pages/tabs or existing CSV outputs unless explicitly instructed.

No secrets in code. No keys in repo. Use .env and document variables.

Prefer small PR-sized changes: implement → run tests → document → only then refactor.

Add/maintain CHANGELOG.md entries per feature.

Definition of Done (DoD):

Feature works locally end-to-end.

Includes a minimal test or validation script.

Includes user instructions in README (how to run + how to verify).

No new warnings/errors in lint/test.

Safety / operations:

Always show what commands you intend to run before running them.

If deleting/moving files: explain impact + list files affected first.